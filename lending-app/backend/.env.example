# FastAPI lending API (backend/). Copy to backend/.env

# Required for POST /v1/eligibility/* (must match frontend LENDING_API_SECRET)
LENDING_API_SECRET=change-me-in-dev

# Optional: allow browser calls during experiments (comma-separated origins)
# LENDING_API_CORS_ORIGINS=http://localhost:3000
